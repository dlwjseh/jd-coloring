# 디자인 스펙 — 화면 1: 사용자 선택

입력 기획서: `docs/01-기획/requirements.md`
대상: iPad·Mac (SwiftUI) · 상태: 화면 1 1차안

## 목업 보는 법
`docs/02-디자인/mockups/` 의 `.svg` 파일을 더블클릭(미리보기/브라우저)하면 바로 열립니다.

| 파일 | 내용 |
|------|------|
| `palette.svg` | 컬러 시스템 |
| `01-user-select.svg` | 메인 — 사용자 선택 화면 |
| `02-add-edit.svg` | 사용자 추가/수정 화면 (= 첫 실행 화면) |
| `03-longpress-delete.svg` | 롱프레스 메뉴 + 삭제 확인 다이얼로그 |
| `04-anim-A1-entry.svg` | 진입 애니메이션 스토리보드 |
| `05-anim-A2-scatter.svg` | 흩어짐+폼 애니메이션 스토리보드 |
| `06-gallery.svg` | **화면 2** — 색칠 도안 갤러리(그리드) |
| `07-gallery-empty.svg` | 화면 2 빈 상태(도안 0개) |
| `08-upload.svg` | 도안 업로드(사진 미리보기 → 저장) |
| `09-canvas.svg` | **화면 3** — 색칠 캔버스 + 컴팩트 도구 독 |
| `10-palette.svg` | 60색 팔레트 펼침 그리드(팝오버) |

---

## 1. 디자인 컨셉 / 무드
- **키워드**: 밝고 둥글고 친근한, 크레용/파스텔 컬러링. 아이도 편하게.
- 모든 요소는 **둥근 모서리**(원·라운드 사각형), 부드러운 그림자, 따뜻한 크림 배경.
- 과하지 않게: 배경은 차분한 크림 그라데이션, 색은 프로필·버튼 등 포인트에만.

## 2. 컬러 시스템 (`palette.svg`)
| 역할 | HEX |
|------|-----|
| 배경 (그라데이션 상/하) | `#FFFFFF` → `#FFF4EC` (거의 화이트, 따뜻한 기 살짝) |
| 잉크(주요 텍스트) | `#3B3A4E` |
| 보조 텍스트 | `#9A8F86` / 옅게 `#B6A89B` |
| 카드 | `#FFFFFF`, 보더 `#EBDFD2` |
| CTA(주요 버튼)·코랄 | `#FF7A59` |
| 위험(삭제) | `#FF5A5F` |
| 프로필 링 6색 | 코랄 `#FF7A59` · 퍼플 `#8A6CFF` · 티일 `#36C5C0` · 옐로 `#FFC740` · 핑크 `#FF6FAF` · 그린 `#5FD08A` |
| 프로필 옅은 채움 | 각 링색의 파스텔 틴트 (예 코랄 `#FFE4DB`) |

프로필 링 색은 **추가된 순서대로 6색을 순환** 배정(사진이 없거나 단색일 때도 구분되게).

## 3. 타이포그래피
- 서체: **SF Pro Rounded**(시스템 라운드). 없으면 시스템 기본.
- 타이틀 800 / 48pt · 서브타이틀 24pt · 이름 라벨 700 / 27pt · 본문·버튼 21~22pt.

## 4. 컴포넌트
- **프로필 원(ProfileCircle)**: 지름 152(=r76), 링 8pt(색), 옅은 틴트 채움, 사진은 원형 마스크. 아래 이름 라벨. 그림자 `dy7/blur11`. 사진 없으면 기본 스마일 일러스트.
- **방울 배경(BubbleBackground)**: 배경 그라데이션 위에 6색 팔레트의 파스텔 방울(원)을 낮은 불투명도(0.09~0.14)로 화면 곳곳에 흩뿌림. 정적·히트테스트 비활성. 화면 크기에 비례 배치(창 리사이즈 대응). *(구현 시 누락됐다가 추가됨 — 모든 화면 배경의 기본 요소로 유지)*
- **추가 버튼(AddButton)**: 우하단 코너 플로팅 원형, 지름 96, 코랄, 흰 `＋`. 아래 "프로필 추가" 캡션.
  - *대안*: 프로필 줄 끝의 점선 `＋` 원(인라인). 코너안을 1차 추천.
- **수정 폼 카드**: 흰 카드 rx36, 내부 = 점선 원형 이미지 픽커("＋ 갤러리에서 사진 선택") + 이름 입력 필드(rx18) + [취소(아웃라인)]/[저장(코랄)].
- **컨텍스트 메뉴 팝오버**: 흰 카드, `✏️ 수정` / `🗑️ 삭제(빨강)`.
- **확인 다이얼로그**: 흰 카드, 아이콘+제목+보조문구 + [취소]/[삭제(빨강)].

## 5. 화면별 레이아웃

### 메인 — 사용자 선택 (`01-user-select.svg`)
- 상단 중앙 타이틀 "오늘은 누가 색칠할까요?" + 서브 "프로필을 선택하세요".
- 중앙: 프로필 원들이 가로 한 줄, 수평 스크롤. 오른쪽 끝 살짝 잘린 프로필로 **더 있음(스크롤)** 암시 + "‹ 좌우로 드래그 ›" 힌트.
- 우하단: 추가 버튼.

### 추가·수정 / 첫 실행 (`02-add-edit.svg`)
- 중앙 폼 카드. 좌·우 가장자리에 흩어진 프로필(A2 종료 상태)이 보임.
- **첫 실행(사용자 0명)** 에는 이 화면이 곧바로 등장. **수정**은 같은 화면을 재사용(기존 값 채워진 상태).

### 롱프레스·삭제 (`03-longpress-delete.svg`)
- ① 롱프레스 → 대상 프로필이 살짝 떠오르고(scale↑) 배경 딤 + 메뉴 팝오버.
- ② [삭제] → 확인 다이얼로그. [삭제]는 빨강.

## 6. 애니메이션 (개발자 인계용)

### A1. 진입 — 순차 슬라이드 인 (`04-anim-A1-entry.svg`)
- 각 프로필이 **화면 오른쪽 바깥 → 제자리**로, **오른쪽→왼쪽** 이동 + 페이드 인.
- **stagger**: index 기반 50~80ms 지연으로 하나씩.
- 정착 시 가벼운 스프링(예: `spring(response:0.45, dampingFraction:0.75)`).
- 트리거: 이 화면 진입 시마다(`onAppear`).

### A2. 흩어짐 + 폼 등장 (`05-anim-A2-scatter.svg`)
- 1단계: 프로필이 좌/우로 갈라져 화면 밖 + 페이드 아웃(가운데 비움).
- 2단계: 폼 카드가 **아래에서 위로** 스윽(+페이드 인). 1·2단계 살짝 오버랩.
- 역방향: 저장/취소 시 폼 내려가 사라지고 프로필 다시 모임.
- 방향 결정: 폼은 **아래→위**로 제안(키보드와 충돌 적고 자연스러움).

> ⚠️ 구현 시: stagger는 타이머 남발 대신 transition+delay로. "매 진입 재생"이라 가볍게 유지 (→ 개발평가자 체크포인트).

## 7. 접근성 / 반응형
- 터치 타깃 ≥ 44pt. 텍스트·배경 대비 확보(잉크/크림).
- **iPad**: 가로/세로 모두. 세로는 프로필 크기 유지하고 줄 길이만 조정.
- **Mac**: 롱프레스 → **우클릭(컨트롤+클릭) 컨텍스트 메뉴**로 대체. 수평 스크롤은 트랙패드 제스처. 창 리사이즈 시 프로필 줄 중앙 정렬 유지.

## 8. 디자이너가 둔 결정 / 열린 질문
- 추가 버튼: **우하단 코너** 1차 추천(인라인 `＋`는 대안).
- 프로필 사진 없을 때: 기본 **스마일 일러스트**(이번엔 단순화). 추후 동물 아바타 세트 검토 가능.
- 폼 등장 방향: **아래→위** 제안.

---

# 화면 2 — 색칠 도안 갤러리

입력 기획서: `docs/01-기획/requirements.md`(화면 2 섹션). 화면 1과 **동일한 배경/그림자/서체/방울**을 그대로 계승.

## 9. 레이아웃 (`06-gallery.svg`)
- **상단 바**: 좌측 **뒤로 버튼**(흰 원 + ‹). 중앙 타이틀 "무엇을 색칠해볼까요?" + 서브 "○○의 색칠 도안 · N개". 우측 **현재 프로필 칩**(작은 원 + 이름, 화면 1 링색 유지).
- **그리드**: 적응형 카드 그리드. 목업 기준 4열(폭에 따라 자동), 카드 **210×210 rx28**, 흰 배경 + 보더 `#EBDFD2` + 카드 그림자(`dy6/blur10`, opacity 0.28), 간격 60.
- **셀 내용**: 도안 일러스트(라인아트) 중앙. 셀 아래 **도안 이름** 라벨.
  - **진행 중**(현재 프로필이 색칠한 도안): 일러스트가 **파스텔로 채워짐** + 좌상단 코랄 "진행 중" 알약 배지. 라벨 잉크색.
  - **미착수**(빈 도안): 일러스트 **잉크 외곽선만**(채움 없음). 라벨 보조색(`#9A8F86`).
- **추가 버튼**: 화면 1과 **완전히 동일**한 우하단 코랄 플로팅 원(＋) + "도안 추가" 캡션.

## 10. 빈 상태 (`07-gallery-empty.svg`)
- 중앙에 점선 캔버스 + 크레용 일러스트. "아직 색칠할 도안이 없어요" + "오른쪽 아래 ＋ 버튼으로 사진을 올려 도안을 만들어요".
- 추가 버튼을 향하는 **점선 가이드 화살표**(코랄)로 첫 행동 유도. (첫 진입·0개일 때만)

## 11. 도안 업로드 (`08-upload.svg`)
- 딤 처리된 배경 위 **시트 카드**(흰색 rx40). 타이틀 "도안 만들기" + 설명 "사진을 골라 색칠 도안으로 추가해요".
- **단일 미리보기** 한 장(흰 박스 rx28, 선택한 이미지 그대로). 그 아래 **"사진 다시 선택"**(코랄 링크).
- 하단 [취소(아웃라인)] / [도안 저장(코랄)].
- ~~라인아트 변환·before/after·"원본 그대로" 토글~~ → **제거(2026-06-01)**. 사진을 가공 없이 그대로 도안으로 등록(사용자가 색칠 도안 이미지를 올린다고 가정).

## 12. 화면 2 애니메이션
- **진입**: 그리드 셀이 화면 1의 A1 톤으로 **순차(stagger) 페이드 + 살짝 스케일 업**(아래/안쪽에서 정착). index 기반 지연, 매 진입 재생은 가볍게.
- 프로필 선택 → 갤러리 전환: 선택 프로필이 상단 칩으로 흐르는 연결감(`matchedGeometryEffect` 검토, 무거우면 단순 크로스페이드).

## 13. 화면 2 결정 / 메모
- "진행 중" 표시는 **배지 + 채움 유무**로 이중 전달(색약 대비).
- 도안 이름은 자동 생성(예: "도안 1") 또는 업로드 시 입력 — 개발 단계에서 간단히(자동 번호) 시작, 입력은 P1.
- 공유 도안 삭제: 기획 확정대로 **강한 확인 다이얼로그**(다른 식구 작업물도 사라짐 명시) 재사용 — 화면 1 삭제 다이얼로그 패턴 계승.

---

# 화면 3 — 색칠 캔버스 (`09-canvas.svg`)

입력 기획서: `docs/01-기획/requirements.md`(화면 3). 화면 1~2의 배경/그림자/서체/방울 계승.

## 14. 레이아웃
- **상단 바**: 좌측 **뒤로 버튼**(저장 후 갤러리로). 중앙 타이틀 "○○ 색칠하기" + 서브 "색칠하면 자동으로 저장돼요". 우측 **"✓ 저장됨" 칩**(연녹 배경, 자동저장 안심 표시).
- **캔버스 카드**: 중앙 큰 흰 카드(rx32, 보더+그림자). 도안을 여백 두고 최대화.
- **도구 독**: 하단 흰 알약 바(rx40, 그림자). 좌→우로 **색 팔레트 · │ · 브러시 굵기 · │ · 지우개**, 각 그룹 아래 작은 라벨(색/굵기/지우개).

## 15. 색칠 표현 (레이어)
- 캔버스 안: **색칠 fill(파스텔) → 그 위에 도안 라인아트(검은 선)** 순서로 그려, 색을 칠해도 **선이 항상 위에 보임**(`.multiply` 오버레이 개념의 시각화).
- **진행 중** 표현: 일부 영역만 칠하고 일부는 흰색(미완)으로 두어 "색칠 중"을 전달. 브러시가 선 밖으로 살짝 번진 자국으로 **브러시 방식**(영역 채우기 아님)임을 암시.
- **펜 커서 힌트**: 아직 안 칠한 영역을 가리키는 Apple Pencil(분홍 팁) — 펜 우선 입력을 암시.

## 16. 도구 독 상세
컴팩트 독: **[현재 색 + 최근색 6] · [색] · [굵기] · [지우개]** (`09-canvas.svg`).
- **현재 색**: 크게 + 진한 잉크 링. **최근 쓴 색 6개**를 옆에 작은 원으로(빠른 재선택). 그룹 라벨 "최근 색".
- **"색" 버튼**: 2×2 컬러 그리드 아이콘. 탭 → **72색 펼침 그리드 팝오버**(`10-palette.svg`).
- **브러시 굵기**: 점 3개(가는→굵은), 선택 단계에 코랄/핑크 링.
- **지우개**: 클래식 지우개 아이콘(분홍 띠+흰 몸통), 탭 시 지우개 모드 토글.
- (P1) **되돌리기/다시**: PencilKit `UndoManager`로 거의 무료 → 독에 ↶/↷ 추가 권장.

### 16-1. 72색 펼침 그리드 (`10-palette.svg`)
- "색" 버튼 위로 떠오르는 **팝오버 카드**(흰색 rx32 + 그림자 + 하단 노치). 제목 "색 고르기".
- **8행 × 9열 = 72색**. 사용자가 제공한 **프리즈마컬러 72색 차트**의 배치 순서를 그대로 따름(대략 노랑→주황·빨강→핑크·보라→파랑→청록·초록→초록·갈색→갈색·무채색→무채색 흐름). 선택 색은 크게+진한 링. 흰색·크림 등 옅은 색은 보더.
- 색 선택 → 팝오버 닫힘 + 최근색에 반영. 바깥 탭으로도 닫힘.

### 16-2. 72색 큐레이션 세트 (개발용 색값 — 프리즈마컬러 공식 근사 HEX, 8행 × 9열)
출처: 사용자가 제공한 프리즈마컬러 Premier 72 근사 HEX 표(2026-06-01 채택). 모니터/스캔 환경에 따라 미세 차이 가능.
PC 코드 행 순서: R1 914·1084·915·916·1002·942·1003·1034·917 / R2 940·918·921·922·926·924·925·930·994 / R3 995·929·928·927·997·939·931·**1104(Amethyst, 차트의 956 자리)**·1008 / R4 932·1007·933·902·906·903·904·1027·1023 / R5 901·992·905·1006·1004·989·1005·913·912 / R6 910·909·911·908·948·1021·1083·941·943 / R7 945·944·937·947·946·1051·1054·1056·1060 / R8 1063·1065·935·1069·1072·1074·938·949·950

- **R1**: `F6E7B5 B98A5A F8EE4A F5D93A E6A13B C89A3C D97A1F C89126 F5B223`
- **R2**: `D7B48A F07C1B F48D6B D9493C C6353B A8262C 7F2434 B53A7A E63D56`
- **R3**: `6F395F F29CB4 F6C0C4 F5C9A7 DCC5AA E9AE8D 4A254E 7A5DA9 B9A3D6`
- **R4**: `6D4AA2 56327A 4C5BB2 3249A5 24406B 2E75C8 5FA6D7 006F8A A7CDEB`
- **R5**: `25365C 8DD7D6 3DB9B5 37A85A BFD636 9CC63B 6AA83D 79C86D 4FA648`
- **R6**: `24854B 34733B 5D6733 2C4B31 4B3428 2D7464 C4AE95 8E6C4D A06638`
- **R7**: `7D4F33 A6533D 5C1F22 4B3528 5A3B2A D2CCC4 9E948B 6F665F D0D4D8`
- **R8**: `979DA3 676E76 1C1C1C D8D3CC AAA39A 7A726A FFFFFF B7BCC3 C8A14A`
- 흰색(`FFFFFF`, PC938)·크림(`F6E7B5`) 등 옅은 색은 표시 시 보더 필요. 이 72색을 앱 내장 상수 배열로 보관(8 × 9). 목업 `10-palette.svg`·`09-canvas.svg` 모두 이 세트로 반영.

## 17. 화면 3 메모 / 열린 질문
- 팔레트 **색 수/구성** 확정 필요(목업은 12색 예시). 아이용 크레용 세트 느낌.
- iPad **펜/손가락 정책**, **줌·팬** 여부, **Mac PencilKit 대체**는 개발 단계에서 확정(기획서 열린 질문 4종).
- 도구 독 위치: 하단 1차. iPad 세로/가로에서 측면 배치 대안 검토 가능.

## 다음 단계
이 시안이 좋으면 `/개발` 로 넘어가 화면을 SwiftUI로 구현. 바꾸고 싶은 색/배치/문구가 있으면 디자이너에게 말해 시안을 갱신.
